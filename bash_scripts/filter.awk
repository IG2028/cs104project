BEGIN{
    FS=","
}
{
    include_line=true

    # if [[ -n "$event_filter" && ! "$line" ~ event_filter ]]; then
    #     include_line=false

    # if [[ -n "$level_filter" && ! "$line" =~ $level_filter ]]; then
    #     include_line=false
    # Jan 18 20:15:30 2005
    

}